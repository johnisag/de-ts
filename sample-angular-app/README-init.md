### init angural app
```bash
npm create vite@latest sample-angular-ap
```

### options
```Angular > Angular ↗ (npm exec @angular/cli@latest new) > install with npm - NO > CSS > SSR - N > zonless - N > Ai tools - N```

### install dependencies
```bash
cd sample-angular-app
npm install
```

### if angular cli is not install, install it
```bash
npm install -g @angular/cli
```

### To start a local development server, run:

```bash
ng serve
```
